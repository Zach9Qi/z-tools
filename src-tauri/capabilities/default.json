{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口的默认权限：仅授予 core:default，新增 plugin 或窗口能力时按最小权限追加",
  "windows": ["main"],
  "permissions": ["core:default"]
}
